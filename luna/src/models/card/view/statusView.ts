export default class StatusView {
    constructor(id: string, name: string, color: string) {
        this.id = id;
        this.name = name;
        this.color = color;
    }

    public id: string;
    public name: string;
    public color: string;
}